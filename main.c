#include <stdio.h>
#include <stdbool.h>

int main(void) {
    bool isRich = false;
    printf("Hello Forsaken World!");

    if (add(1, 1) == 2) {
        isRich = true;
    }

    if (rich) {
        printf("Squeeze every drop of value from your workers!");
    }
    else {
        printf("Eat the rich!");
    }
    
    return 0;
}
